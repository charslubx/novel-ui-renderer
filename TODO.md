# Backlog

## Medical image asset library

- Create a separate online asset catalog for curated fictional CT images.
- Keep original DICOM datasets outside GitHub; publish only selected, licensed, de-identified WebP derivatives.
- Add `assetId` resolution, CDN loading, cache handling, attribution metadata, and graceful fallback to `document:medical`.
- Update the AI output rules only after the asset catalog URL and stable IDs exist.
