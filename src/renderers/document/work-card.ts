import type { NovelUIRenderer } from "../../core/renderer-registry";
import { element } from "../../utils/dom";
import { renderDefaultPersonAvatar } from "../shared/person-avatar";
import common from "../../styles/common.css?inline";
import styles from "../../styles/identity.css?inline";
interface WorkCardProps{organization:string;department?:string;fullName:string;title:string;employeeId:string;validUntil?:string;accessLevel?:string;}
export const WorkCardRenderer:NovelUIRenderer<WorkCardProps>={component:"document",variant:"work-card",styles:common+styles,validate(value):value is WorkCardProps{const p=value as WorkCardProps;return !!p&&[p.organization,p.fullName,p.title,p.employeeId].every((x)=>typeof x==="string");},render(props){const root=element("article","novel-ui work-card"),header=element("header","work-card__header");header.append(element("div","work-card__organization",props.organization),element("div","work-card__kind","EMPLOYEE IDENTIFICATION"));const body=element("section","work-card__body");body.append(renderDefaultPersonAvatar(),element("div","work-card__name",props.fullName),element("div","work-card__title",props.title));const details=element("div","work-card__details");[["EMPLOYEE ID",props.employeeId],["DEPARTMENT",props.department],["ACCESS",props.accessLevel],["VALID UNTIL",props.validUntil]].filter(([,value])=>value).forEach(([label,value])=>{const field=element("div","work-card__field");field.append(element("div","work-card__label",label),element("div","work-card__value",value));details.append(field);});body.append(details);root.append(header,body,element("footer","work-card__footer",`ID ${props.employeeId} · FICTIONAL PROP`));return root;}};
